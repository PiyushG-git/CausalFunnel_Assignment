PORT=5000
MONGODB_URI=mongodb://localhost:27017/causalfunnel
FRONTEND_URL=http://localhost:5173

# Optional API key to protect dashboard read endpoints (GET /api/sessions, /api/heatmap)
# POST /api/events remains public (tracker collection endpoint, no key needed)
# Leave empty to disable auth (good for local dev)
API_KEY=
